<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:SmartApplyHub.Mobile.Pages"
       x:Class="SmartApplyHub.Mobile.AppShell"
       Title="SmartApply Hub"
       FlyoutBackgroundColor="White"
       Shell.BackgroundColor="{StaticResource Primary}"
       Shell.ForegroundColor="White"
       Shell.TitleColor="White"
       Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Dashboard" Route="Dashboard">
        <ShellContent ContentTemplate="{DataTemplate pages:DashboardPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Search Jobs" Route="JobSearch">
        <ShellContent ContentTemplate="{DataTemplate pages:JobSearchPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Apply with AI" Route="JobAnalyze">
        <ShellContent ContentTemplate="{DataTemplate pages:JobAnalyzePage}" />
    </FlyoutItem>

    <FlyoutItem Title="Applications" Route="Applications">
        <ShellContent ContentTemplate="{DataTemplate pages:ApplicationsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Bulk Apply" Route="BulkApply">
        <ShellContent ContentTemplate="{DataTemplate pages:BulkApplyPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Profile" Route="Profile">
        <ShellContent ContentTemplate="{DataTemplate pages:ProfilePage}" />
    </FlyoutItem>

    <FlyoutItem Title="Subscription" Route="Subscription">
        <ShellContent ContentTemplate="{DataTemplate pages:SubscriptionPage}" />
    </FlyoutItem>

</Shell>
